name: Build and Publish Python Packages

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (build only, no publish)'
        required: false
        default: 'false'
        type: boolean

permissions:
  contents: read

env:
  PYTHON_VERSION: '3.11'

jobs:
  # Build and publish imspy-core first (no dependencies)
  publish-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry

      - name: Build imspy-core
        run: |
          cd packages/imspy-core
          poetry build

      - name: Publish imspy-core
        if: ${{ github.event_name == 'release' || inputs.dry_run != 'true' }}
        env:
          POETRY_PYPI_TOKEN_PYPI: ${{ secrets.IMSPY_PYPI_TOKEN }}
        run: |
          cd packages/imspy-core
          poetry config pypi-token.pypi $POETRY_PYPI_TOKEN_PYPI
          poetry publish

  # Build and publish imspy-predictors (depends on imspy-core)
  publish-predictors:
    runs-on: ubuntu-latest
    needs: publish-core
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry

      - name: Build imspy-predictors
        run: |
          cd packages/imspy-predictors
          poetry build

      - name: Publish imspy-predictors
        if: ${{ github.event_name == 'release' || inputs.dry_run != 'true' }}
        env:
          POETRY_PYPI_TOKEN_PYPI: ${{ secrets.IMSPY_PYPI_TOKEN }}
        run: |
          cd packages/imspy-predictors
          poetry config pypi-token.pypi $POETRY_PYPI_TOKEN_PYPI
          poetry publish

  # Build and publish remaining packages (depend on imspy-core and imspy-predictors)
  publish-remaining:
    runs-on: ubuntu-latest
    needs: publish-predictors
    strategy:
      matrix:
        package: [imspy-search, imspy-simulation, imspy-vis]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry

      - name: Build ${{ matrix.package }}
        run: |
          cd packages/${{ matrix.package }}
          poetry build

      - name: Publish ${{ matrix.package }}
        if: ${{ github.event_name == 'release' || inputs.dry_run != 'true' }}
        env:
          POETRY_PYPI_TOKEN_PYPI: ${{ secrets.IMSPY_PYPI_TOKEN }}
        run: |
          cd packages/${{ matrix.package }}
          poetry config pypi-token.pypi $POETRY_PYPI_TOKEN_PYPI
          poetry publish
