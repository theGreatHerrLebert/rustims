[workspace]
members = [
    "ms-chem",
    "mscore",
    "rustdf",
    "rustms",
    "timsim-chem",
    "timsim-schema",
    "timsim-cli",
    "imspy_connector",
    "imsjl_connector",
    "tims-viewer",
    "tims-viewer/web"
]

resolver = "2"

[profile.release]
debug = true
overflow-checks = true
lto = "thin"
panic = "abort"
